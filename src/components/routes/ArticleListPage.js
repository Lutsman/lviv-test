import * as React from 'react';

import {ArticleList} from "../ArticleList";

export const ArticleListPage = props => {
    const {} = props;

    return (
        <div>
            <ArticleList/>
        </div>
    );
};

import * as React from 'react';

import {NewArticle} from "../NewArticle";

export const NewArticlePage = props => {
    const {} = props;

    return (
        <div>
            <NewArticle/>
        </div>
    );
};
import * as React from 'react';

import {Article} from "../Article";

export const ArticlePage = props => {
    const {} = props;

    return (
        <div>
            <Article/>
        </div>
    );
};
